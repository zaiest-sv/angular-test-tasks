import { Routes } from '@angular/router';
import { SearchFormComponent } from './search-form.component';

export const SEARCH_FORM_ROUTES: Routes = [
    { path: '', component: SearchFormComponent }
];
